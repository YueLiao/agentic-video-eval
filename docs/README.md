# docs

`design.html` — 当前设计说明（中文，面向评审）。

GitHub 不会渲染仓库里的 HTML，只会显示源码。要看渲染效果：

- 克隆后本地打开，或
- 开启 GitHub Pages（Settings → Pages → Source: main / docs），
  之后访问 `https://<user>.github.io/agentic-video-eval/design.html`
