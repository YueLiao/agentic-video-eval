# V3 重新设计:把「评估程序」本身交给模型来写

## 0. 对 V2 的诚实评估

现有实现的问题不是没做完,是**架构选错了层次**:

| 现状 | 问题 |
|---|---|
| 8 个手写 skill,每个 3–7 个硬编码动作 | 动作空间表达不了组合逻辑。「跟踪物体→拟合弹道→若残差大则放大到偏离最大帧」写不出来 |
| tool 主要是裁剪与拼图 | 是**呈现**工具,不是**感知**工具。没有可编程的感知原语 |
| code agent 只在文档里 | 最有威力的那件事没建 |
| prompt 照着 gemma 的能力写 | 换成强模型不会变强,换成弱模型会崩。不是 plug-and-play |
| 关键词路由 + 能力缺口检查 | 是查表,不是推理 |

**根本问题**:我把「评估流程」写死在代码里,只把少数选择点留给模型。
但一个视频该怎么评,本身就是个需要推理的问题——**评估程序应该是被生成的,不是被选择的**。

---

## 1. 核心转变:从「选动作」到「写程序」

### 现在

```python
# skill 提供菜单,模型选一个
actions = [zoom_hands, zoom_face, skeleton, ...]
choice = vlm.pick(actions)          # ≤4 轮
```

### V3

```python
# 模型针对这条输入**写出**评估程序
program = planner.synthesize(condition, taxonomy, substrate_api, capability_profile)

# 程序在沙箱中执行,其中 ask() 是对 VLM 的调用
def evaluate(clip, ask):
    tracks = track_objects(clip, phrases=["陶土", "双手"])
    rigid  = rigidity(tracks["陶土"])          # 计算
    if rigid.cv_p90 > 0.15:                   # 由计算结果决定下一步
        t = rigid.worst_frame
        ev = paired_view(clip, tracks["陶土"].bbox_at(t), t)
        v  = ask("这处形变是黏土的正常塑性,还是刚体崩坏?", ev,
                 schema=GRADED_FINDING)        # 只在计算判不了时才问模型
        if v.grade != "clean":
            yield finding("rigidity_violation", v, evidence=[ev, rigid])
    ...
```

这个差别是本质的:
* **计算与感知交错**。程序可以先算再看、根据算出来的东西决定看哪里。
* **控制流是内容相关的**。陶轮视频和爆炸视频生成的程序完全不同。
* **可审计、可 diff、可复用**。程序是文本,能被人读、被版本控制、被复核。
* **VLM 调用是精准的**。只在计算无法判定处调用,且带着计算好的证据。

### 为什么这才是 code agent 的正确用法

我原来把 code agent 设计成「一个 agent 可以调用的沙箱工具」。那是弱用法。
**强用法是:评估程序本身就是模型写的代码。** 前者是给流程加一个补丁,
后者是让模型决定流程。

---

## 2. 三层架构

```
┌─ Layer 3  裁决层 ────────────────────────────────────┐
│  执行程序;程序中的 ask() 由目标 VLM 应答            │
│  + 验证轮(反证 / 自一致 / 交叉模型)                  │
└──────────────────────────────────────────────────────┘
┌─ Layer 2  规划层 (强 LLM,离线,每 condition 一次) ───┐
│  condition + taxonomy + 感知 API + 能力画像          │
│    → 生成评估程序 (Python)                           │
│  程序被冻结、校验、版本化                            │
└──────────────────────────────────────────────────────┘
┌─ Layer 1  基底层 (无 LLM,类型化,可组合) ────────────┐
│  感知原语 + 几何/物理算子 + 证据渲染 + ask()         │
└──────────────────────────────────────────────────────┘
```

### Layer 1:感知基底(不是「工具列表」,是一套可编程 API)

关键区别:现在的 tool 是「按名字调用、返回一个字典」;基底是**类型化的、可组合的对象**。

```python
# 时空选择
clip.frames(t0, t1, stride=1) -> Frames
clip.at(t) -> Frame
Frames.roi(bbox) -> Frames          # 链式
Frames.where(pred) -> Frames        # 条件筛选

# 感知
detect(frames, phrases: list[str]) -> Detections   # 开放词表
track(frames, init) -> Track                       # 带置信度与丢失区间
segment(frames, prompt) -> Masks
pose(frames) -> Skeletons
depth(frames) -> DepthMaps
embed(frames | regions) -> Embeddings              # 用于身份/相似度

# 派生量(纯几何,无模型)
flow(frames) -> FlowField
residual(frames) -> ResidualMap        # 运动补偿后
rigidity(track) -> RigidityStat
trajectory(track) -> TrajectoryFit     # 含弹道拟合与残差
invariants(skeletons) -> AnatomyStat
spectrum(frames) -> SpectralStat

# 证据渲染(把测量变成 VLM 能看的东西)
view.paired(frame, bbox) / view.ladder(...) / view.filmstrip(...)
view.overlay(frames, detections | skeletons | flow)
view.plot(series, ...)                 # 曲线也是证据

# 唯一的模型入口
ask(question, evidence, schema, *, mode="judge"|"describe"|"compare") -> Answer
```

`ask()` 可以在生成的程序里被调用,**这是整个设计的枢纽**——它让「计算」和「感知」
在同一个控制流里交错,而不是分成两个阶段。

### Layer 2:程序合成

规划器收到:
* condition(可能含首帧/参考图)
* 缺陷分类法(带每类的分档判据)
* 感知 API 的类型签名与文档
* **目标 VLM 的能力画像**(见 §3)
* 该 condition 推导出的不变量(有人→解剖学;有落体→弹道)

产出一个 Python 函数,加上它的**评估契约**:声明它会覆盖哪些 aspect、
每个 aspect 依赖哪些证据、预算上限。

校验(全部确定性,不过就打回重生成):
* AST 白名单(只能调基底 API,禁 IO/网络/反射)
* 静态资源估计(帧解码量、ask() 次数、GPU 算子调用)
* 覆盖检查:声明要评的 aspect 是否真的有代码路径产出结论
* 干跑:在 2 秒占位视频上执行,确保不崩、不死循环

### Layer 3:执行与验证

执行程序,记录完整轨迹(每次 ask 的证据与回答、每个中间量)。
然后跑验证轮——**验证策略也应当由能力画像决定**:
* 强模型:自生成验证问句(chain-of-verification)+ 反证
* 中等模型:反证 + 多相位共识
* 弱模型:只做多相位共识,并把置信度封顶

---

## 3. Plug-and-play:能力画像

「换任何 VLM 都能用」不能靠一套 prompt 兼容所有模型,而要靠**探测 + 适配 + 校准**。

### 3.1 能力探测(首次接入一个模型时跑一次)

一组小而确定的探针,每个只测一件事:

| 探针 | 测什么 | 影响 |
|---|---|---|
| `max_images` | 单次调用能塞多少图仍正确计数 | 证据包大小 |
| `native_video` | 是否接受原生视频输入 | 运动类判断走视频还是帧序列 |
| `grounding` | 能否输出可用的归一化 bbox | 开放词表检测走 VLM 还是 CV |
| `temporal_order` | 能否读出烧录帧号并正确排序 | filmstrip 是否可用 |
| `fine_detail` | 448px 裁剪上能否数清手指 | 缩放梯度的起点 |
| `schema_fidelity` | 复杂嵌套 schema 的一次通过率 | 输出契约的复杂度上限 |
| `instruction_depth` | 长 prompt 中段的约束是否被遵守 | 约束该放 prompt 还是代码兜底 |
| `self_consistency` | 同一问题 k 次采样的一致率 | 是否需要投票、投几次 |
| `calibration` | 在锚样本上的分档是否与金标一致 | 分数映射的偏移与缩放 |

产出 `CapabilityProfile`,规划器与执行器都读它。
**这直接解决了我现在的问题**:prompt 照着 gemma 写,换模型不会变强。

### 3.2 锚定校准(让不同 VLM 的结果可比)

plug-and-play 的真正难点不是「能跑」,是**换了模型分数还能不能比**。

做法:维护一组**锚样本**——每类缺陷若干条,已由多人评定档位。
任何新接入的 VLM 先在锚集上跑,得到它的分档偏移与展宽,
之后把它的输出映射到统一刻度上。没有这一步,换模型等于换尺子。

---

## 4. 充分挖掘强 VLM 的手段

现在只用了「一次提问、一次回答」。可用而未用的:

| 手段 | 为什么有效 | 何时启用 |
|---|---|---|
| **多轮追问** | 让模型自己决定还要看什么,而不是我预设菜单 | `instruction_depth` 高的模型 |
| **自生成验证** | 模型为自己的每条结论写验证问句,再独立回答 | 强模型;比外部反证更贴合它的推理 |
| **自一致投票** | k 次采样取多数,分歧率本身是置信度 | `self_consistency` 低的模型必须用 |
| **对抗辩论** | 两个实例分别为「有缺陷/无缺陷」辩护,第三方裁决 | 高价值样本、争议样本 |
| **参考锚定** | 同时给出已知 clean 与已知 severe 的同类样本 | 分档校准,尤其跨模型 |
| **原生视频输入** | 运动类判断在帧序列上天然吃亏 | `native_video` 为真时 |
| **主动定位盲区** | 用注入缺陷探测该模型在哪类上不可靠,路由绕开 | 接入时一次性 |

关键点:**这些不该全开**。哪些开、开几次,由能力画像和预算决定——
这才是「智能」该体现的地方,而不是把所有技巧堆上去。

---

## 5. 面向下一代:防饱和

现有 benchmark 会饱和,因为它们检查**固定清单**。下一代模型会把清单上的都做对。
三个机制:

### 5.1 自适应难度

预算按**信息量**分配,而不是均匀。两个模型在某维度上分数接近时,
自动加深该维度的检查(更多相位、更高放大、更多验证轮),直到能分开或达到预算上限。
分不开就报「在此预算下不可分」——这是有用的结论,不是失败。

### 5.2 开放词表缺陷发现

除了核对 22 类已知缺陷,增加一条:
**「除上述之外,你是否观察到其他不自然之处?」** 收集自由描述,
跨视频聚类,反复出现且能被独立复现的,提升为候选新缺陷类型,经人工确认后进分类法。

**分类法必须能生长**,否则它会随生成模型进步而过时。

### 5.3 相对而非绝对

绝对分数在头部必然饱和(所有模型都 9.5+)。
原生支持成对比较与 Bradley-Terry 排名,绝对分只用于诊断与门槛。

---

## 6. 下一代视频生成会考什么

现有维度(符不符合 prompt、稳不稳定、好不好看)会被做满。真正的区分点:

| 能力 | 为什么需要「算」而不只是「看」 |
|---|---|
| **交互中的物理正确性** | 不只是「球会掉」,而是碰撞是否守动量。需要跟踪 + 物理拟合 |
| **长时程状态一致** | 10s+ 中被遮挡的物体再出现时状态是否延续。需要跨时段 re-id |
| **组合式指令遵循** | 嵌套/条件指令(「如果 A 发生则 B」)。需要事件检测 + 时序逻辑 |
| **精细控制符合度** | 相机路径、时间点、精确计数。需要轨迹估计与比对 |
| **世界模型自洽** | 遮挡推理、画外持存。需要深度序 + 跟踪 |

**这些都无法只靠「问 VLM 一句」得到**,必须由程序把测量做出来再交给 VLM 判断。
这正是 code agent 的价值所在。

---

## 7. 迁移路径

不是推倒重来。V2 中可保留的:

| 保留 | 理由 |
|---|---|
| 缺陷分类法与分档判据 | 22 类 × 4 档带分界,是硬内容 |
| 三轴打分(severity/extent/salience)与合成规则 | 已验证能产生 44/48 分数 |
| 反证机制与举证责任设计 | 已验证把撤回率从 100% 修到合理区间 |
| 逐项表态(沉默≠无罪) | 已验证能拉开区分度 |
| 多相位共识 | 针对已测出的采样噪声 |
| 证据渲染(paired/ladder/filmstrip) | 成为基底的 view 层 |
| 静默失败护栏 | 血泪换来的 |

需要重建的:
* skill 的动作菜单 → 程序合成
* 关键词路由 → 由程序的覆盖声明驱动
* 固定 prompt → 能力画像驱动的自适应
* 沙箱 code 工具 → 提升为核心执行模型

**建议顺序**:
1. 基底 API(把现有 tool 重构成类型化可组合的对象)
2. 能力探针 + 画像(这是 plug-and-play 的前提,且能立刻回答「gemma 到底能干什么」)
3. 程序合成 + 校验 + 沙箱执行
4. 在现有 MQ benchmark 上对照 V2 与 V3

第 2 步可以最先做,因为它同时是**诊断工具**:
现在所有结论都来自 gemma,能力画像能告诉我们哪些结论是被模型能力限制的,
哪些是架构问题。
