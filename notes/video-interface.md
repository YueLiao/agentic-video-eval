# 接口用错了 —— 2026-09-11

一整天在 image 接口里加证据(8→16→32 帧、运动曲线、差分图、裁剪放大),六轮全部不动。
换成模型自带的 video 接口,第一次跑就是质变。

## 为什么 image 接口加不动

gemma-4 的 `processor_config.json`:

```
image_processor:  max_soft_tokens 280   ← 整张图共享,无论塞多少帧
video_processor:  max_soft_tokens 70 × num_frames 32 = 2240  ← 每帧独占
```

池化单元 48px。所以:

| 版式 | 每帧有效分辨率 |
|---|---|
| 8 列条带 3242×456 | 264px |
| 2×2 拼 1030×1030 | 384px |
| 1×2 并排 1536×768 | 528px |
| **video 32 帧** | **约 400px × 32 帧** |

**总视觉预算差 8 倍。** 而且 video 是唯一携带**速率**的通道——运动质量主要就是速率,
抽样恰恰是毁掉速率的那一步。这解释了清单模式里"卡顿/打滑/速度/幅度"四项在 1220 条
上报出率为 **0%**:证据里根本没有答案。

## 结果

```
                        ladder(166对)   upset(689对)   顺序翻转
容量/档位先验             76.7%            0%            —
CV 12 维线性              55.6%          42.6%           —
逐处放大链              52.8–55.3%          —            —
抽样帧成对比较            48–58%            —          53–65%
VLM + video              82.2%          64.1%          22%
```

**顺序翻转从 53–65% 掉到 22%** —— 这是性质上的变化,多数判断终于经得起换边。
模型的理由也随之改变,开始讲"重心转移""跳跃感""僵硬瞬间"——整体运动属性,
放大的局部裁剪永远说不出这些。

## 两个靶子,两种失败模式,都通过

**ladder**(同配方只差容量,指纹受控):82.2% vs 容量先验 76.7%。
McNemar p=0.424(赢 15 输 10,n=90)——**这一项统计上不成立**,不能声称胜过先验。

**upset**(弱档赢强档,先验按构造 = 0%):**64.1%,z=+6.48,n=529**。
同一子集上平凡基线全在随机附近(永远选 A 50.9%、永远选 B 49.1%),
预测分布(a:261 b:268)与真值分布(a:269 b:260)几乎吻合,529 对全部顺序一致。

CV 模型在这里是 42.6%(低于随机,在骑先验)。**所以 video 版的判断不是指纹的另一种表达。**

## 教训

**在优化怎么用一个接口之前,先确认用的是不是对的接口。**
capability probe 已经会探图像预算(`token_capped` / `native_capped`),
下一条必须加:有没有 video 通道、它的预算是多少、每帧多少 token。
这是 plug-and-play 的 harness 必须先问、而我最后才问的问题。

## 完整 val_ac(998 对),以及与训练好的 RM 的头对头

分层是**均匀**的——三个"档位先验无用"的切片全部显著:

```
切片          对数  非平局  acc(无平局)  表态      方向     z      档位先验  翻转
全部          998   587     56.2%   470/587   70.2%  +8.76    81.2%   23%
family seed   306   110     51.8%    83/110   68.7%  +3.40      --     30%
family S-S    177   104     46.2%    85/104   56.5%  +1.19      --     20%
family W-S    167   137     62.8%   104/137   82.7%  +6.67    90.4%   24%
upset 爆冷     61    61     60.7%    52/61    71.2%  +3.05     0.0%   15%
档位相同      245   245     51.8%   194/245   65.5%  +4.31      --     21%
```

`acc(无平局) 56.2%` 偏低是因为交换检查会**弃权**(117/587 判为位置偏置),
而该指标不许弃权、弃权全算错。强制表态(取第一种顺序)后是 **65.9%**。

### 与 VideoAlign 训练的 RM 头对头(bench p1 同 499 对、同指标、均强制表态)

```
切片            n    VideoAlign RM   本仓 video      差       z
overall        322      65.5          65.2        -0.3    +5.46

family seed     63      39.7          69.8       +30.1    +3.15
upset 爆冷      31      64.5          83.9       +19.4    +3.77
family S-S      56      53.6          51.8        -1.8    +0.27
family M-S      73      58.9          52.1        -6.8    +0.35
family W-M      39      94.9          82.1       -12.8    +4.00
family W-S      74      85.1          71.6       -13.5    +3.72
ordercheck     155      79.4          62.6       -16.8    +3.13
```

**总分打平,构成完全相反。** 指纹无用的切片我们大幅领先(seed +30.1、爆冷 +19.4);
指纹有用的切片他们大幅领先(W-S −13.5、顺风 −16.8)。

微调的 RM 学到的主要是**认生成器**;零训练、走 video 通道的 VLM 学到的是**看运动**。

**seed 那一格对做 RM 最要紧**:单策略的 RL/BoN 喂进来的就是同模型不同采样,
他们 39.7%(低于随机),我们 69.8%。在 RM 真正要用的分布上差 30 个点且方向相反。

未解决:`S-S` 51.8% / `M-S` 52.1`%`,两边都不行。同档不同模型仍是空白。

**推论**:两条路线互补,分界线正好是"指纹有没有用",合并应同时高于两者。
