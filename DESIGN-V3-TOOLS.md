# V3 · 工具体系与模型自写工具

## 0. 组织原则

一个 VLM 的表现被两件事框死:**它的上下文里有什么**,和**你问了什么问题**。
工具存在的唯一理由是改变这两者。所以每个工具都必须能回答「我解除哪个限制」:

| 限制 | 表现 | 工具类别 |
|---|---|---|
| **采样盲区** | 缺陷所在的帧压根没进上下文 | A · 注意力引导 |
| **分辨率不足** | 进了上下文但被压到不可判读 | B · 视图合成 |
| **先验缺失** | 看得见但不知道那是不对的 | C · 测量与不变量 |
| **问法不对** | 全都到位了但问题问错了 | D · 提问模式 |

第四类是我此前完全忽略的。同一份证据,问「这段视频质量如何」和
问「左手在 f95 与 f103 之间,指节数量是否改变?」得到的信息量差一个量级。

---

## A · 注意力引导:决定看哪里

| 工具 | 产出 | 为什么需要 |
|---|---|---|
| `suspicion_map` ✅ | 排序的 (t, 区域) 待查清单 | 运动补偿残差为主,已实现 |
| `change_points` | 内容突变的时刻 | 抓切换、身份跳变 |
| `detector_dropout` | 检测器置信度塌陷的帧 | **检测器失效本身就是信号** |
| `novelty_frames` | 相对本片自身分布的离群帧 | 不需要先验的通用异常 |
| **`differential_map`** | **两条同 prompt 视频的差异图** | **配对评测时,差异本身就定位了该看哪** |
| `saliency` | 人眼会看哪 | 决定 salience 轴,也决定缺陷值不值得罚 |
| `periodicity_scan` | 周期性信号的相位断点 | 轮子、步态、摆动的不连续 |

`differential_map` 是这批里最有价值的:MQ benchmark 全是成对比较,
**两条视频的差异区域数量远少于各自的可疑区域**,直接把搜索空间压掉一个量级。

---

## B · 视图合成:决定怎么看

已有:`paired_view` `scale_ladder` `filmstrip` `temporal_extent` `contrast_pair` ✅

需要补的:

| 工具 | 做什么 | 解决什么 |
|---|---|---|
| **`predicted_overlay`** | 把物理预测(弹道/刚体变换)渲染叠加在实际画面上 | 把"违反物理"从数字变成**看得见的偏离** |
| `warp_residual_view` | 运动补偿前后并排 + 残差热力 | 让模型看见"运动解释不掉的部分" |
| `aligned_pair` | 两条视频按时间戳对齐并排 | 配对评测的标准视图 |
| `difference_amplify` | 两条视频的差异放大 | 细微差异可见化 |
| `track_ribbon` | 被跟踪物体沿时间的裁剪条带 | 一眼看完某个物体的一生 |
| `skeleton_overlay` ✅ | 骨架画在画面上 | 让模型**核查检测器**而非信任它 |
| `signal_strip` | 曲线与帧缩略图对齐成一条带 | 把数值和画面绑在同一张图上 |

`predicted_overlay` 值得展开:与其告诉模型「vy_residual_ratio=0.89」,
不如**把重力预测的轨迹画上去**,让它看见球该在哪、实际在哪。
数值需要模型信任,图像可以让它自己核实。

---

## C · 测量与不变量:提供模型没有的先验

已有:`rigidity` `freefall` `anatomy_invariants` `permanence` `flow` `sharpness` ✅

按「陈述一个对真实世界恒真的事实,然后测量偏离」这个模式,还能加很多:

| 不变量 | 陈述 | 测量 |
|---|---|---|
| **动量守恒** | 碰撞前后总动量不变 | 跟踪双方,比对碰撞前后动量和 |
| **周期一致** | 轮子/步态的周期恒定 | 跟踪信号 FFT,查相位断点 |
| **遮挡序一致** | A 挡 B 则 A 更近,不会无故互换 | 深度序 + mask 重叠 |
| **支撑闭合** | 静止物体必有支撑接触 | 深度 + 分割,查接触点 |
| **光影一致** | 同场景所有阴影指向同一光源 | 从各物体阴影估计光向,查方差 |
| **颜色恒常** | 同一物体颜色不随时间无故变化 | 跟踪区域的色度轨迹 |
| **计数稳定** | 物体数量不会无故增减 | 跨帧计数序列(比单帧计数更可靠) |
| **相机自洽** | 相机轨迹的重投影误差应当低 | SfM 残差 |
| **尺度恒常** | 刚体尺寸不随位置无故变化 | 跟踪尺寸 / 深度 的比值 |
| **谱指纹** | 自然图像的径向频谱斜率在一定范围 | FFT 斜率异常 |
| **时间自相似** | 真实视频的帧间自相似矩阵有特征结构 | 异常平滑/周期即生成痕迹 |

这类工具的共同优点:**几何事实,不随生成模型进步而失效**,
且**不可伪造**——刚体两点距离恒定是定义,不是训练分布。

---

## D · 提问模式:同样的证据,不同的问法

这是我此前完全没做的一层。`ask()` 不该只有一种模式:

| 模式 | 形式 | 何时用 |
|---|---|---|
| `ask.judge` ✅ | 给证据,要判定 | 默认 |
| **`ask.describe_then_judge`** | 先要求描述看到什么,再基于自己的描述判定 | **显著降低幻觉**:先落到具体观察,再下结论 |
| **`ask.enumerate`** | 强制逐项穷举而非自由汇报 | 已验证有效(逐项表态) |
| **`ask.compare`** | 成对,而非各自打分 | 绝对判断不可靠时;benchmark 本身就是成对的 |
| **`ask.locate`** | 要求返回坐标而非描述 | 可被 IoU 验证,比文字描述更可核查 |
| **`ask.verify`** | 给一个断言,要求独立验证 | 反证、自生成验证 |
| **`ask.discriminate`** | 给一真一假,要求指出哪个是生成的 | **强制模型说出判据**,比问"有没有缺陷"更尖锐 |
| **`ask.counterfactual`** | "如果这是真实拍摄,这里应该是什么样?" | 让模型调用它的自然视频先验 |
| **`ask.dialogue`** | 多轮,模型可以要求看更多 | 强模型;把选择权真正交出去 |

`ask.discriminate` 特别值得试:**判别比生成容易**。
与其问「这段视频有什么缺陷」(开放、易幻觉),
不如把生成片段和真实片段并排问「哪个是 AI 生成的,依据是什么」——
模型必须说出具体判据才能作答,而那个判据就是我们要的缺陷描述。

---

## E · 模型自写工具

### 三个层次

| 层次 | 形态 | 现状 |
|---|---|---|
| L1 | 一次性分析代码,跑完即弃 | 设计过,未建 |
| L2 | **评估程序**由模型写 | V3 主线 |
| L3 | **模型写出可复用的新工具**,验证后进入工具库 | 本节 |

### L3 怎么运作

规划器在写评估程序时,发现基底缺一个测量(例如「转轮的角速度是否均匀」),
于是**写一个新工具**:

```python
@synthesized(needs=["track"], returns="PeriodicityStat")
def rotation_uniformity(track: Track, fps: float) -> PeriodicityStat:
    """跟踪点绕质心的角度序列,查角速度的方差与相位断点。"""
    ang = np.unwrap(np.arctan2(track.y - track.cy, track.x - track.cx))
    w = np.diff(ang) * fps
    return PeriodicityStat(mean_rate=w.mean(), cv=w.std()/abs(w.mean()),
                           breaks=find_phase_breaks(ang))
```

### 关键:验证,否则它比没有更糟

一个未经验证的自写工具会**制造出看起来精确的错误数字**,
而下游会把它当事实锚定。所以每个自写工具必须过四关:

1. **静态检查** —— AST 白名单,只能调基底 API,禁 IO/网络/反射;资源上界估计。
2. **合成验证** —— **用缺陷注入器生成金标**。要测「角速度均匀性」?
   就用 `inject.time_warp` 制造已知的变速,看它测不测得出来。
   **这是现有注入器的第二个用途,而且比第一个更重要。**
3. **不变性检查** —— 对不该影响结果的扰动(采样相位、分辨率、水平翻转)结果应稳定;
   不稳定就说明它在测量伪影而非内容。
4. **成本检查** —— 实测耗时与调用量,超预算的降级或拒收。

四关都过才进缓存;被多个 program 复用且持续通过的,升格进永久工具库。
**没过的工具不是丢弃,而是记录失败原因**——那本身是关于什么可测、什么不可测的知识。

### 为什么这条路成立

因为我们**有金标生成器**。多数「让模型写工具」的方案卡在无法验证,
只能靠人工复核。而缺陷注入器可以为任意新工具即时合成带标注的测试集:
要验证一个测闪烁的工具,就注入已知强度的闪烁。
**注入器把「模型自写工具」从不可控变成可验收的。**

---

## F · 全面激发模型能力的手段

工具之外,还有一层是**怎么用模型**:

| 手段 | 机制 | 代价 |
|---|---|---|
| 自一致投票 | k 次采样取多数,分歧率即置信度 | k 倍调用 |
| 自生成验证 | 模型为自己每条结论写验证问句再独立回答 | 2 倍 |
| 对抗辩论 | 两实例分别辩护,第三方裁决 | 3 倍,仅用于争议样本 |
| 参考锚定 | 同时给已知 clean / severe 的同类样本 | 需锚样本库 |
| 真伪判别 | 生成片段与真实片段并排 | 需配对真实素材 |
| 原生视频输入 | 运动类判断不再受帧序列所限 | 需模型支持 |
| 多轮追问 | 模型自己决定还要看什么 | 上下文增长 |
| 盲区探测 | 用注入集测出该模型在哪类缺陷上不可靠 | 一次性 |

**这些不该全开。** 开哪些、开几次由能力画像与预算决定——
自一致的模型不需要投票,撑不住两轮 schema 的模型做不了辩论。
**取舍本身就是「智能」该体现的地方。**

盲区探测尤其值得一次性投入:用注入器测出「gemma 在闪烁上可靠、在穿模上不可靠」,
之后穿模就交给几何测量、闪烁就交给模型。**按模型的强弱分配任务,而不是一视同仁。**

---

## G · 建设顺序

1. **基底类型系统** —— 把现有工具重构成可链式组合的类型化对象(`Frames` `Track` `Detections`)。
   自写工具的前提是有一套稳定、可组合、有类型签名的 API 可写。
2. **`ask` 的多模式** —— describe_then_judge / enumerate / compare / locate / verify /
   discriminate。这一层最便宜,收益立刻可测。
3. **差分注意力** —— `differential_map` + `aligned_pair`,直接服务成对 benchmark。
4. **自写工具 + 四关验证** —— 建立在 1 与注入器之上。
5. **盲区探测** —— 用注入集给每个模型出一份「哪类缺陷该信它、哪类不该」的表。
